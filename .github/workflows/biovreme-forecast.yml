name: Biovreme

on:
  schedule:
    - cron: '15 6 * * *'   # 6:15 UTC daily, po kakovost-zraka-forecast.yml
  workflow_dispatch:        # allow manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Generate biovreme stran
        run: python3 tools/generate_biovreme_page.py

      - name: Commit & push if changed
        run: |
          git config user.name  "meteorec-bot"
          git config user.email "bot@meteorec.si"
          git add biovreme/ || true
          git diff --cached --quiet && echo "Ni sprememb." && exit 0
          git commit -m "seo: posodobi biovreme $(date -u +%Y-%m-%d) [skip ci]"
          git push
